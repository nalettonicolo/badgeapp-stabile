# Dipendenze
node_modules/
**/node_modules/

# Build / cache
.expo/
dist/
build/
*.log

# OS / editor
.DS_Store
Thumbs.db

# Backup locale (non serve in repo)
backup/

# Repo git annidato (cartella stabile aveva init locale)
BadgeApp_stabile_netlify/.git/

# Legacy / copie locali (non in repo)
BADGEAPP/
timbrature_app/
index.txt
index_copia_copilot.txt
